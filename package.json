{
  "name": "kenjanek-agile-tracker",
  "version": "0.1.0",
  "description": "Simple Agile Tracker (Kanban) with REST API and JSON storage",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test test/api.test.js",
    "test:ui": "playwright test"
  },
  "dependencies": {
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
